# Environment Variables for Taajr Application

# Secret key for Flask sessions (generate a random string for production)
SECRET_KEY=your-secret-key-here

# Mail Configuration (if different from default in app.py)
# MAIL_USERNAME=taajr.services@gmail.com
# MAIL_PASSWORD=your-app-password-here
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587

# Database URL (for production with PostgreSQL/MySQL)
# DATABASE_URL=postgresql://user:password@host:port/database
